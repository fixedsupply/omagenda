# Omagenda skill

Not yet implemented: Phase 4 (see AGENTS.md).

Will describe the `omagenda` CLI (agenda, next, parse, add, calendars,
sync --json) so a Claude Code session can answer calendar questions and
add events the same way the panel does. See PLAN.md §6.7.
