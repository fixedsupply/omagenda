"""Microsoft Graph bridge.

OAuth 2.0 against an Entra app registration, delta queries, Windows-to-IANA
timezone mapping, JSON<->VEVENT mapping per ARCHITECTURE.md §11. Built on
the same Bridge interface the Google bridge proves out.

Not yet implemented: Phase 5 (see AGENTS.md). Deferred first if the budget
runs short (PLAN.md §9).
"""
